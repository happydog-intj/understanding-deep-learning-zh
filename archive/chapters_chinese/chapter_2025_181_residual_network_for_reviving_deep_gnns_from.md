# 第2025章：用于从深层图神经网络中恢复性能的181层残差网络

*页码：524–541*

510 参考文献  
Zaheer, M., Reddi, S., Sachan, D., Kale, S., & Kumar, S. (2018). 自适应非凸优化方法。《神经信息处理系统进展》（Neural Information Processing Systems），31，9815–9825。93  
Zaslavsky, T. (1975). 面向超平面划分的空间：关于由超平面划分空间所得区域数的计数公式。《美国数学学会备忘录》（Memoirs of the American Mathematical Society），38, 40。  
Zhang, H., Dauphin, Y. N., & Ma, T. (2019a). Fixup 初始化：无需归一化的残差学习。《国际学习表征会议》（International Conference on Learning Representations）。114, 205  
Zhang, H., Goodfellow, I., Metaxas, D., & Odena, A. (2019b). 自注意力生成对抗网络。《国际机器学习会议》（International Conference on Machine Learning），7354–7363。300  
Zhang, H., Hsieh, C.-J., & Akella, V. (2016a). Hogwild++：一种新型去中心化异步随机梯度下降机制。《IEEE 国际数据挖掘会议》（IEEE International Conference on Data Mining），629–638。114  
Zhang, H., Xu, T., Li, H., Zhang, S., Wang, X., Huang, X., & Metaxas, D. N. (2017d). StackGAN：基于堆叠式生成对抗网络的文本到照片级真实感图像合成。《IEEE/CVF 国际计算机视觉会议》（IEEE/CVF International Conference on Computer Vision），5907–5915。301, 302  
Zhang, J., & Meng, L. (2019). GResNet：用于从“暂停动画”状态中复苏深度图神经网络的图残差网络。arXiv:1909.05729。263  
Zhang, J., Shi, X., Xie, J., Ma, H., King, I., & Yeung, D.-Y. (2018a). GaAN：面向大规模时空图学习的门控注意力网络。《人工智能中的不确定性》（Uncertainty in Artificial Intelligence），339–349。263  
Zhang, J., Zhang, H., Xia, C., & Sun, L. (2020). Graph-BERT：仅需注意力机制即可学习图表示。arXiv:2001.05140。263  
Zhang, K., Yang, Z., & Başar, T. (2021a). 多智能体强化学习：理论与算法精选综述。《强化学习与控制手册》（Handbook of Reinforcement Learning and Control），321–384。399  
Zhai, S., Talbott, W., Srivastava, N., Huang, C., Goh, H., Zhang, R., & Susskind, J. (2021). 无注意力机制的 Transformer。235  
Zhang, A., Lipton, Z. C., Li, M., & Smola, A. J. (2023). 《动手学深度学习》（Dive into Deep Learning）。剑桥大学出版社。15  
Zhang, C., Bengio, S., Hardt, M., Recht, B., & Vinyals, O. (2017a). 理解深度学习需重新思考泛化能力。《国际学习表征会议》（International Conference on Learning Representations）。156, 404, 419  
Zhang, C., Ouyang, X., & Patras, P. (2017b). ZipNet-GAN：通过生成对抗神经网络推断细粒度移动通信流量模式。《新兴网络实验与技术国际会议》（International Conference on emerging Networking EXperiments and Technologies），363–375。300  
Zhang, H., Cisse, M., Dauphin, Y. N., & Lopez-Paz, D. (2017c). mixup：超越经验风险最小化。《国际学习表征会议》（International Conference on Learning Representations）。160  
Zhang, L., & Agrawala, M. (2023). 为文本到图像扩散模型添加条件控制。arXiv:2302.05543。371  
Zhang, M., & Chen, Y. (2018). 基于图神经网络的链路预测。《神经信息处理系统进展》（Neural Information Processing Systems），31，5171–5181。262  
Zhang, M., Cui, Z., Neumann, M., & Chen, Y. (2018b). 一种端到端的图分类深度学习架构。《AAAI 人工智能会议》（AAAI Conference on Artificial Intelligence），4438–4445。262, 265  
Zhang, Q., & Chen, Y. (2021). 扩散归一化流。《神经信息处理系统进展》（Neural Information Processing Systems），34，16280–16291。372  
Zhang, R. (2019). 使卷积网络再度具备平移不变性。《国际机器学习会议》（International Conference on Machine Learning），7324–7334。182, 183  

本作品受知识共享署名-非商业性使用-禁止演绎（CC-BY-NC-ND）许可协议约束。（©）麻省理工学院出版社。

参考文献 511  
Zhang, R., Isola, P., & Efros, A. A. (2016b). 彩色图像着色（Colorful image colorization）. 欧洲计算机视觉会议（European Conference on Computer Vision），649–666. 159  
Zhou, C., Ma, X., Wang, D., & Neubig, G. (2019). 双语词嵌入的密度匹配（Density matching for bilingual word embedding）. ACL 人类语言技术会议（ACL Human Language Technologies），1588–1598. 323  
Zhang, S., Tong, H., Xu, J., & Maciejewski, R. (2019c). 图卷积网络：一项全面综述（Graph convolutional networks: A comprehensive review）. 《计算社会网络》（Computational Social Networks），6(1)，1–23. 262  
Zhou, H., Alvarez, J. M., & Porikli, F. (2016a). 少即是多：面向紧凑型卷积神经网络（Less is more: Towards compact CNNs）. 欧洲计算机视觉会议（European Conference on Computer Vision），662–677. 416  
Zhang, S., Zhang, C., Kang, N., & Li, Z. (2021b). iVPF：面向高效无损压缩的数值可逆体积保持流（iVPF: Numerical invertible volume preserving flow for efficient lossless compression）. IEEE/CVF 计算机视觉与模式识别会议（IEEE/CVF Computer Vision & Pattern Recognition），620–629. 323  
Zhou, J., Cui, G., Hu, S., Zhang, Z., Yang, C., Liu, Z., Wang, L., Li, C., & Sun, M. (2020a). 图神经网络：方法与应用综述（Graph neural networks: A review of methods and applications）. 《人工智能开放》（AI Open），1，57–81. 262  
Zhang, X., Zhao, J., & LeCun, Y. (2015). 面向文本分类的字符级卷积网络（Character-level convolutional networks for text classification）. 神经信息处理系统大会（Neural Information Processing Systems），28，649–657. 182  
Zhou, K., Huang, X., Li, Y., Zha, D., Chen, R., & Hu, X. (2020b). 面向更深层图神经网络的可微分组归一化（Towards deeper graph neural networks with differentiable group normalization）. 神经信息处理系统大会（Neural Information Processing Systems），33，4917–4928. 265  
Zhao, H., Jia, J., & Koltun, V. (2020a). 探索自注意力机制用于图像识别（Exploring self-attention for image recognition）. IEEE/CVF 计算机视觉与模式识别会议（IEEE/CVF Computer Vision & Pattern Recognition），10076–10085. 238  
Zhou, L., Du, Y., & Wu, J. (2021). 基于点-体素扩散的三维形状生成与补全（3D shape generation and completion through point-voxel diffusion）. IEEE/CVF 国际计算机视觉会议（IEEE/CVF International Conference on Computer Vision），5826–5835. 370  
Zhao, J., Mathieu, M., & LeCun, Y. (2017a). 基于能量的生成对抗网络（Energy-based generative adversarial network）. 国际学习表征会议（International Conference on Learning Representations）. 300  
Zhou, T., Krahenbuhl, P., Aubry, M., Huang, Q., & Efros, A. A. (2016b). 基于三维引导循环一致性的稠密对应学习（Learning dense correspondence via 3D-guided cycle consistency）. IEEE/CVF 计算机视觉与模式识别会议（IEEE/CVF Computer Vision & Pattern Recognition），117–126. 302  
Zhao, L., & Akoglu, L. (2020). PairNorm：应对图神经网络中的过平滑问题（PairNorm: Tackling oversmoothing in GNNs）. 国际学习表征会议（International Conference on Learning Representations）. 265  
Zhou, Y.-T., & Chellappa, R. (1988). 利用神经网络计算光流（Computation of optical flow using a neural network）. IEEE 国际神经网络会议（IEEE International Conference on Neural Networks），71–78. 181  
Zhao, L., Mo, Q., Lin, S., Wang, Z., Zuo, Z., Chen, H., Xing, W., & Lu, D. (2020b). UCTGAN：基于无监督跨空间迁移的多样化图像修复（UCTGAN: Diverse image inpainting based on unsupervised cross-space translation）. IEEE/CVF 计算机视觉与模式识别会议（IEEE/CVF Computer Vision & Pattern Recognition），5741–5750. 238  
Zhou, Z., & Li, X. (2017). 图卷积：一种高阶且自适应的方法（Graph convolution: A high-order and adaptive approach）. arXiv:1706.09916. 263  
Zhao, S., Song, J., & Ermon, S. (2017b). InfoVAE：在变分自编码器中平衡学习与推断不变性（InfoVAE: Balancing learning and inference in variational autoencoders）. AAAI 人工智能会议（AAAI Conference on Artificial Intelligence），5885–5892. 346  
Zhou, Z., Rahman Siddiquee, M. M., Tajbakhsh, N., & Liang, J. (2018). UNet++：一种用于医学图像分割的嵌套式 U-Net 架构（UNet++: A nested U-Net architecture for medical image segmentation）. 深度学习在医学图像分析研讨会（Deep Learning in Medical Image Analysis Workshop），3–11. 205  
Zhao, S., Song, J., & Ermon, S. (2017c). 面向变分自编码模型的更深层次理解（Towards deeper understanding of variational autoencoding models）. arXiv:1702.08658. 346  
Zheng, C., Cham, T.-J., & Cai, J. (2021). TFill：基于 Transformer 架构的图像补全方法（TFill: Image completion via a transformer-based architecture）. arXiv:2104.00845. 238  
Zhu, C., Ni, R., Xu, Z., Kong, K., Huang, W. R., & Goldstein, T. (2021). GradInit：面向稳定高效训练的神经网络初始化学习（GradInit: Learning to initialize neural networks for stable and efficient training）. 神经信息处理系统大会（Neural Information Processing Systems），34，16410–16422. 113  
Zheng, G., Yang, Y., & Carbonell, J. (2017). 卷积归一化流（Convolutional normalizing flows）. arXiv:1711.02255. 323  
Zheng, Q., Zhang, A., & Grover, A. (2022). 在线决策 Transformer（Online decision transformer）. 国际机器学习会议（International Conference on Machine Learning），162，27042–27059. 399  
Zhong, Z., Zheng, L., Kang, G., Li, S., & Yang, Y. (2020). 随机擦除数据增强（Random erasing data augmentation）. AAAI 人工智能会议（AAAI Conference on Artificial Intelligence），13001–13008. 159  
Zhu, J., Krähenbühl, P., Shechtman, E., & Efros, A. A. (2016). 自然图像流形上的生成式视觉操控（Generative visual manipulation on the natural image manifold）. 欧洲计算机视觉会议（European Conference on Computer Vision），597–613. 302  
Zhu, J., Shen, Y., Zhao, D., & Zhou, B. (2020a). 面向真实图像编辑的域内 GAN 反演（In-domain GAN inversion for real image editing）. 欧洲计算机视觉会议（European Conference on Computer Vision），592–608. 302  

草稿：请将勘误发送至 udlbookmail@gmail.com。

512 参考文献  
Zhu, J.-Y., Park, T., Isola, P., & Efros, A. A.  
(2017). 使用循环一致性对抗网络的非配对图像到图像翻译（Unpaired image-to-image translation using cycle-consistent adversarial networks）. IEEE/CVF 国际计算机视觉会议（International Conference on Computer Vision），2223–2232. 297, 302  

Zhu, X., Su, W., Lu, L., Li, B., Wang, X., & Dai, J. (2020b). 可变形 DETR：面向端到端目标检测的可变形 Transformer（Deformable DETR: Deformable transformers for end-to-end object detection）. 国际学习表征会议（International Conference on Learning Representations）. 238  

Zhuang, F., Qi, Z., Duan, K., Xi, D., Zhu, Y., Zhu, H., Xiong, H., & He, Q. (2020). 迁移学习综述（A comprehensive survey on transfer learning）. 《IEEE 汇刊》（Proceedings of the IEEE），109(1)，43–76. 159  

Ziegler, Z., & Rush, A. (2019). 用于离散序列建模的隐式归一化流（Latent normalizing flows for discrete sequences）. 国际机器学习会议（International Conference on Machine Learning），7673–7682. 323, 324  

Zong, B., Song, Q., Min, M. R., Cheng, W., Lumezanu, C., Cho, D., & Chen, H. (2018). 深度自编码高斯混合模型用于无监督异常检测（Deep autoencoding Gaussian mixture model for unsupervised anomaly detection）. 国际学习表征会议（International Conference on Learning Representations）. 345  

Zou, D., Cao, Y., Zhou, D., & Gu, Q. (2020). 梯度下降法优化过参数化的深度 ReLU 网络（Gradient descent optimizes over-parameterized deep ReLU networks）. 《机器学习》（Machine Learning），109，467–492. 405  

Zou, D., Hu, Z., Wang, Y., Jiang, S., Sun, Y., & Gu, Q. (2019). 面向深度与大规模图卷积网络训练的层依赖重要性采样（Layer-dependent importance sampling for training deep and large graph convolutional networks）. 神经信息处理系统会议（Neural Information Processing Systems），32，11247–11256. 264  

Zou, H., & Hastie, T. (2005). 利用弹性网络进行正则化与变量选择（Regularization and variable selection via the elastic net）. 《英国皇家统计学会会刊：B 辑》（Journal of the Royal Statistical Society: Series B），67(2)，301–320. 156  

Zou, Z., Chen, K., Shi, Z., Guo, Y., & Ye, J. (2023). 目标检测二十年：一项综述（Object detection in 20 years: A survey）. 《IEEE 汇刊》（Proceedings of the IEEE）. 184  

本作品遵循知识共享署名-非商业性使用-禁止演绎（CC-BY-NC-ND）许可协议。© MIT 出版社。

索引  
ℓ₂ 范数，443　对抗攻击，414  
ℓ∞ 范数，443　对抗损失，293，302  
ℓₚ 范数，443　对抗训练，149  
`<cls>` 标记，221　仿射函数，447  
1×1 卷积，174，181　聚合后验分布，341，342  
1D 卷积，163　AlexNet，174  
1D 卷积网络，162–170，182　算法微分，106  
2D 卷积网络，170–174　AMSGrad，93  
3D U-Net，205　祖先采样，460  
3D 卷积网络，182　argmax 函数，438  
argmin 函数，438  
ACGAN，289　人工道德主体，425  
动作值，378　伦理影响代理，425  
激活，35　显式伦理代理，425  
激活函数，25，38　完全伦理代理，425  
拼接 ReLU，38　隐式伦理代理，425  
ELU，38　异步数据并行，114  
GeLU，38　ATARI 2600 基准测试，387  
HardSwish，38　注意力机制  
漏泄 ReLU（leaky ReLU），38　加性注意力，235  
逻辑 Sigmoid，38　作为路由的注意力，235  
参数化 ReLU（PReLU），38　图注意力网络，258  
ReLU，25，38　键-值注意力，235  
缩放指数线性单元（SELU），113　局部注意力，237  
SiLU，38　内存压缩注意力，235  
Softplus，38　内存压缩注意力（拼写错误原文为 memory-compresssed），237  
Swish，38　乘性注意力，235  
tanh，38　挤压-激励网络（Squeeze-and-Excitation Network），235  
激活归一化，113　合成器（Synthesizer），235  
ActNorm，113　数据增强，152–154，159  
在图神经网络中的应用，265　自相关函数，442  
自动编码器，345　变分自动编码器，327–348  
修正自动编码器，93　自动翻译，226  
AdamW，94，155　自动化偏见，430  
自动化工作岗位替代，13　自回归流，312–314，324  
辅助分类器 GAN（ACGAN），289　平均池化，171，181  
渐近记号，439　反向传播，97–106，113  

草稿：请将勘误发送至 udlbookmail@gmail.com。

514 索引  
内向树图（in-branching graphs），107　InstructGPT，399  
有向无环图（acyclic graph），116　经典范式（classical regime），129  
装袋法（bagging），146　分类（classification）  
巴拿赫不动点定理（Banach fixed-point theorem），315　二元分类（binary），2，64–66  
基线（baseline），392　ImageNet，174–176，181  
批（batch），85　多类分类（multiclass），2，67–69  
批归一化（batch normalization），192–194，203　文本分类（text），221  
替代方案（alternatives to），205　分类器引导（classifier guidance），365，371  
幽灵批归一化（ghost），203　无分类器引导（classifier-free guidance），366  
蒙特卡洛批归一化（Monte Carlo），203　CLIP，238  
其作用原理（why it helps），204　[cls] token，221  
批强化学习（batch reinforcement learning），395　ClusterGCN，264  
批重归一化（batch renormalization），203　CNN，参见卷积网络（convolutional network）  
贝叶斯公式（Bayes’ rule），451　列空间（column space），444  
贝叶斯神经网络（Bayesian neural networks），150　计算机视觉（computer vision）  
贝叶斯优化（Bayesian optimization），135　图像分类（image classification），174  
集束搜索（beam search），224　目标检测（object detection），177  
行为策略（behavior policy），385　语义分割（semantic segmentation），178  
贝尔曼方程（Bellman equations），380　凹函数（concave function），441  
伯努利分布（Bernoulli distribution），65　权力集中（concentration of power），431  
BERT，219–222　概念漂移（concept shift），135  
β-VAE，343，346　条件生成对抗网络（conditional GAN），289，301  
β-伯努利赌博机（beta-Bernoulli bandit），135　条件生成（conditional generation），7，289，291，371  
偏差（测试误差的组成部分）（bias (component of test error)），122　条件概率（conditional probability），450  
偏差与公平性（bias and fairness），13，422–425　条件变分自编码器（conditional VAE），345  
通过“不感知”实现公平性（fairness through unawareness），424　连续分布（continuous distribution），449  
缓解方法（mitigation），424　压缩映射（contraction mapping），315  
受保护属性（protected attribute），424　控制网络（control network），371  
分离性（separation），424　凸函数（convex function），80，91  
偏置参数（bias parameter），36　凸区域（convex region），441  
偏置向量（bias vector），49　卷积（convolution）  
偏差–方差权衡（bias-variance trade-off），125　1×1 卷积，174，181  
大 O 记号（big-O notation），439　1D 卷积，163  
BigBird，237　自适应卷积（adaptive），183  
双射（bijection），440　空洞卷积（atrous），181  
二元分类（binary classification），64–66　通道卷积（channel），165  
二元交叉熵损失（binary cross-entropy loss），66　深度可分离卷积（depthwise），181  
二项式系数（binomial coefficient），442　膨胀卷积（dilated），164，181  
BlockDrop，202　特征图（feature map），165  
BOHB，136　分数步长卷积（fractionally strided），279  
玻尔兹曼策略（Boltzmann policy），400　门控卷积（gated），181  
自助聚合（bootstrap aggregating），146　分组卷积（grouped），181  
字节对编码丢弃（BPE dropout），234　引导卷积（guided），183  
字节对编码（byte pair encoding），218，234　卷积核（kernel），163  
填充（padding），164  
容量（capacity），29，46，125，134　部分卷积（partial），181  
有效容量（effective），134　可分离卷积（separable），181  
Rademacher 复杂度（Rademacher complexity），134　步长（stride），164  
Vapnik-Chervonenkis 维数（Vapnik-Chervonenkis dimension），134　转置卷积（transposed），172，181  
胶囊网络（capsule network），235　有效卷积（valid），164  
级联扩散模型（cascaded diffusion model），368，370　卷积层（convolutional layer），161，165  
类别分布（categorical distribution），67　卷积网络（convolutional network），161–185  
通道（channel），165　1D 卷积网络，162–170，182  
通道分离卷积（channel-separate convolution），181　2D 卷积网络，170–174  
聊天机器人（chatbot），399　3D 卷积网络，182  
ChatGPT，399　AlexNet，174  
本作品遵循知识共享署名-非商业性使用-禁止演绎（CC-BY-NC-ND）许可协议。（C）麻省理工学院出版社。

索引 515  
通道数变化，174　Transformer，222–227  
下采样，171　变分自编码器（VAE），338  
早期应用，180　解码算法，234  
测地卷积神经网络（Geodesic CNN），265　深度卷积生成对抗网络（Deep Convolutional GAN），279  
GoogLeNet，181　深度双流网络（Deep Dueling Network），398  
归纳偏置（Inductive Bias），170　深度神经网络（Deep Neural Network），41–55  
LeNet，180　矩阵表示法，49  
Network-in-Network，181　引入深度的必要性，418–419  
上采样，172　线性区域数量，50, 52  
VGG，176　与浅层网络的对比，49–51  
可视化，184　深度 Q 网络（Deep Q-Network），386–388, 397  
卷积正交初始化器（Convolution Orthogonal Initializer），183　DeepSets，263  
代价函数，参见损失函数（loss function）　度矩阵（Degree Matrix），257  
耦合流（Coupling Flow），311–312, 324　去噪扩散隐式模型（Denoising Diffusion Implicit Model），365, 371  
耦合函数（Coupling Function），323　DenseNet，195, 205  
协方差（Covariance），455　深度效率（Depth Efficiency），50, 53  
协方差矩阵（Covariance Matrix），455, 457　神经网络深度（Depth of Neural Network），46  
对角形式，457　逐深度卷积（Depthwise Convolution），181  
满秩形式，457　设计正义（Design Justice），434  
球面形式，457　行列式（Determinant），445  
协变函数（Covariant Function），162　对角协方差矩阵（Diagonal Covariance Matrix），457  
协变量偏移（Covariate Shift），135　对角增强（Diagonal Enhancement），257  
交叉注意力（Cross-Attention），227　对角矩阵（Diagonal Matrix），446  
交叉协方差图像 Transformer（Cross-Covariance Image Transformers），238　微分同胚（Diffeomorphism），440  
交叉熵（Cross-Entropy），71–72　微分（Differentiation）  
交叉验证（Cross-Validation），134　前向模式（Forward Mode），117  
Crossformer，238　反向模式（Reverse Mode），116  
维数灾难（Curse of Dimensionality），129, 135　DiffPool，265  
Cutout，158, 183　扩散模型（Diffusion Model），368, 349–373  
CycleGAN，293–296　应用，370  
级联式（Cascaded），370  
DALL·E-2，11, 371　分类器引导（Classifier Guidance），365, 371  
数据（Data）　无分类器引导（Classifier-Free Guidance），366  
结构化数据（Structured Data），17　似然计算（Computing Likelihood），370  
表格数据（Tabular Data），17　条件分布（Conditional Distribution），354–356  
训练集（Training Set），118　控制网络（Control Network），371  
数据增强（Data Augmentation），152–154, 159　DALL·E-2，371  
数据漂移（Data Drift），135　DDIM，371  
概念偏移（Concept Shift），135　解码器（Decoder），349, 356–357  
协变量偏移（Covariate Shift），135　去噪扩散隐式模型（Denoising Diffusion Implicit Model），365  
先验偏移（Prior Shift），135　编码器（Encoder），349–356  
数据并行（Data Parallelism）　证据下界（Evidence Lower Bound），357–359  
异步方式，114　前向过程（Forward Process），350–356  
同步方式，114　图像生成，363  
数据隐私（Data Privacy），429　GLIDE，371  
数据集（Dataset）　Imagen，371  
ImageNet，174　实现细节，363–368  
MNIST，292　质量提升，370  
MNIST-1D，118　速度优化，364, 370  
DaViT，232, 238　核（Kernel），351–353  
DCGAN，279　损失函数（Loss Function），359–360  
DDIM，371　边缘分布（Marginal Distribution），353–354  
致命三重奏问题（Deadly Triad Issue），397　噪声条件增强（Noise Conditioning Augmentation），370  
决策 Transformer（Decision Transformer），395　噪声调度（Noise Schedule），350  
解码器（Decoder）　概率流常微分方程（Probability Flow ODE），371  
卷积网络，179　与其他模型的关系，372  
扩散模型，349  

草稿：请将勘误发送至 udlbookmail@gmail.com。

516 索引  
重参数化（reparameterization），361–363　双注意力视觉Transformer（dual attention vision transformer），232，238  
逆向过程（reverse process），356–357　对偶-原始图卷积神经网络（dual-primal graph CNN），264  
文本到图像生成（text-to-image），371　死亡ReLU问题（dying ReLU problem），38  
训练（training），357–361　动态规划法（dynamic programming method），383  
更新（update），350  
与其他生成模型的对比（vs. other generative models），370　早停法（early stopping），145，157  
空洞卷积（dilated convolution），164，181　边（edge），240  
空洞率（dilation rate），165　有向边（directed），241  
狄拉克δ函数（Dirac delta function），441　嵌入（embedding），243  
折扣因子（discount factor），375　无向边（undirected），241  
离散分布（discrete distribution），449　边图（edge graph），260  
判别模型（discriminative model），23　有效模型容量（effective model capacity），134  
判别器（discriminator），276　特征谱（eigenspectrum），445  
解耦潜在空间（disentangled latent space），271　特征值（eigenvalue），445  
解耦（disentanglement），342，346　弹性网络惩罚项（elastic net penalty），156  
分布间距离（distance between distributions），460–462　证据下界（ELBO），参见“证据下界”  
正态分布间距离（distance between normal distributions），462　逐元素流（elementwise flow），310–311，323  
知识蒸馏（distillation），416，419　ELIZA效应（ELIZA effect），429  
分布式训练（distributed training），114　ELU激活函数（ELU），38  
数据并行（data parallelism）　EM算法（EM algorithm），347  
异步式（asynchronous），114　嵌入（embedding），218  
同步式（synchronous），114　就业（employment），430  
流水线模型并行（pipeline model parallelism），114　编码器（encoder），179  
张量模型并行（tensor model parallelism），114　卷积网络编码器（convolutional network），179  
分布（distribution）　扩散模型（diffusion model），349  
伯努利分布（Bernoulli），65　Transformer编码器（transformer），219–222  
分类分布（categorical），67　变分自编码器编码器（VAE），338  
高斯混合分布（mixture of Gaussians），75，328　编码器-解码器网络（encoder-decoder network），179  
多元正态分布（multivariate normal），457　编码器-解码器自注意力机制（encoder-decoder self-attention），227  
正态分布（normal），61　编码器-解码器Transformer（encoder-decoder transformer），226  
泊松分布（Poisson），76　集成方法（ensemble），145，157，158  
单变量正态分布（univariate normal），457　快速几何集成（fast geometric），158  
冯·米塞斯分布（von Mises），74　快照集成（snapshot），158  
散度（divergence）　随机权重平均（stochastic weight averaging），158  
Jensen-Shannon散度（Jensen-Shannon），461　熵SGD（entropy SGD），158  
Kullback-Leibler散度（Kullback-Leibler），71，461　环境影响（environmental impact），430  
多样性（diversity），434　幕（episode），384  
点积（dot product），444　轮次（epoch），85  
点积自注意力（dot-product self-attention），208–215　ε-贪心策略（epsilon-greedy policy），385  
键（key），210　等变性（equivariance），162  
矩阵表示（matrix representation），212　群等变性（group），182  
查询（query），210　排列等变性（permutation），239  
缩放点积（scaled），214　旋转等变性（rotation），182  
值（value），208　平移等变性（translation），182  
双重下降现象（double descent），127，134，413　伦理影响（ethical impact agent），425  
按轮次划分的双重下降（epoch-wise），134　伦理学（ethics），12–14，421–436  
双重深度Q网络（double DQN），388　人工道德主体（artificial moral agency），425  
双重Q学习（double Q-learning），388　伦理影响（ethical impact agent），425  
下采样（downsample），171　显式伦理智能体（explicit ethical agent），425  
DropConnect，265　完整伦理智能体（full ethical agent），425  
DropEdge，265　隐式伦理智能体（implicit ethical agent），425  
Dropout，147　自动化偏见（automation bias），430  
Cutout，158，183　就业岗位自动化（automation of jobs），13  
在最大池化层中（in max pooling layer），183　偏差与公平性（bias and fairness），13，422–425  
蒙特卡洛Dropout（Monte Carlo），158　通过忽视实现公平（fairness through unawareness），424  
循环Dropout（recurrent），158　缓解措施（mitigation），424  
空间Dropout（spatial），158，183　受保护属性（protected attribute），424  

本作品受知识共享署名-非商业性使用-禁止演绎（CC-BY-NC-ND）许可协议约束。（C）麻省理工学院出版社。

索引 517  
分离（separation），424　前馈网络（feed-forward network），35  
案例研究（case study），431　少样本学习（few-shot learning），224，225  
权力集中（concentration of power），431　滤波器（filter），163  
多样性（diversity），434　微调（fine-tuning），151，152  
就业影响（employment），430　拟合 Q 学习（fitted Q-learning），385  
环境影响（environmental impact），430　拟合（fitting），参见“训练（training）”  
存在性风险（existential risk），14　Fixup，205  
可解释性（explainability），13，426–427　极小值平坦度（flatness of minimum），412  
LIME，427　泛洪法（flooding），159  
知识产权（intellectual property），429　焦点损失（focal loss），73  
滥用（misuse）　前向模式微分（forward-mode differentiation），117  
军事化（militarization），13　Fréchet 距离（Fréchet distance），462  
人工智能滥用（misuse of AI），427　正态分布间的 Fréchet 距离（Fréchet distance between normals），462  
数据隐私（data privacy），429　Fréchet 起始距离（Fréchet inception distance），273  
人脸识别（face recognition），427　分数步长卷积（fractionally strided convolution），279  
欺诈（fraud），428　欺诈（fraud），428  
军事化（militarization），428　Frobenius 范数（Frobenius norm），443  
政治干预（political interference），428　正则化（regularization），140，155  
道德技能退化（moral deskilling），430　全协方差矩阵（full covariance matrix），457  
科学传播（scientific communication），433　完全伦理智能体（full ethical agent），425  
透明性（transparency），425–426　全批量梯度下降（full-batch gradient descent），85  
功能（functional），426　全连接（fully connected），36  
运行（run），426　函数（function），438，440  
结构（structural），426　双射（bijection），440  
价值对齐（value alignment），421–427　微分同胚（diffeomorphism），440  
内对齐问题（inner alignment problem），422　指数函数（exponential），441  
外对齐问题（outer alignment problem），422　伽马函数（gamma），441  
委托-代理问题（principal agent problem），422　单射（injection），440  
科学的价值中立理想（value-free ideal of science），432　对数函数（logarithm），441  
欧几里得范数（Euclidean norm），443　满射（surjection），440  
证据（evidence），452　功能透明性（functional transparency），426  
证据下界（evidence lower bound），331–336  
性质（properties），334　Gabor 模型（Gabor model），80  
重述（reformulation），334　伽马函数（gamma function），441  
界紧性（tightness of bound），334　生成对抗网络（GAN），参见“生成对抗网络（generative adversarial network）”  
存在性风险（existential risk），14　门控卷积（gated convolution），181  
期望（expectation），453–456　门控多层感知机（gated multi-layer perceptron），235  
期望运算规则（rules for manipulating），453　高斯分布（Gaussian distribution），参见“正态分布（normal distribution）”  
期望最大化（expectation maximization），347　GeLU，38  
经验回放（experience replay），387　泛化（generalization），118，403  
可解释性（explainability），13，426–427　决定泛化能力的因素（factors that determine），411–415  
LIME，427　生成对抗网络（generative adversarial network），276–303  
显式伦理智能体（explicit ethical agent），425　ACGAN，289  
梯度爆炸问题（exploding gradient problem），108　对抗损失（adversarial loss），293  
残差网络中的梯度爆炸（in residual networks），192　条件 GAN（conditional），289，301  
探索-利用权衡（exploration-exploitation trade-off），374　条件生成（conditional generation），289–291  
指数函数（exponential function），441　CycleGAN，293–296  
扩展 Transformer 构造（extended transformer construction），237　DCGAN，279  
训练难度（difficulty of training），280  
人脸识别（face recognition），427　判别器（discriminator），276  
因子分析（factor analysis），345　用 GAN 编辑图像（editing images with），302  
因子 VAE（factor VAE），347　生成器（generator），276  
公平性（fairness），参见“偏差（bias）”　图像翻译（image translation），291–296  
无意识公平性（fairness through unawareness），424　InfoGAN，291  
快速几何集成（fast geometric ensembles），158　GAN 的逆向（inverting），302  
特征图（feature map），165　最小二乘法（least squares），300  
特征金字塔网络（feature pyramid network），183　损失函数（loss function），277，300  

草稿：勘误请发送至 udlbookmail@gmail.com。

518 索引  
mini-batch 判别（mini-batch discrimination），289、301  实际应用（real world），240  
模式坍缩（mode collapse），280、301  任务（tasks），246  
模式丢失（mode dropping），280  类型（types），241  
多尺度（multiple scales），301  图注意力网络（graph attention network），258、263  
PatchGAN，292  图同构网络（graph isomorphism network），264  
Pix2Pix，292  图拉普拉斯矩阵（graph Laplacian），262  
渐进式增长（progressive growing），287、301  图神经网络（graph neural network），240–268  
SRGAN，293  数据增强（augmentation），265  
StyleGAN，296–298、301  批处理（batches），264  
训练技巧（tricks for training），300  对偶-原始图卷积神经网络（dual-primal graph CNN），264  
截断技巧（truncation trick），289  图注意力网络（graph attention network），258  
VEEGAN，301  GraphSAGE，263  
Wasserstein，281–286、300  高阶卷积层（higher-order convolutional layer），263  
梯度惩罚（gradient penalty），286  MixHop，263  
权重裁剪（weight clipping），286  MoNet，263  
生成模型（generative model），7、23、223、270  归一化（normalization），265  
理想性质（desirable properties），270  过度平滑（over-smoothing），265  
性能量化（quantifying performance），272  池化（pooling），265  
生成器（generator），276  正则化（regularization），265  
测地线卷积神经网络（geodesic CNN），265  残差连接（residual connection），263、266  
几何图（geometric graph）  谱方法（spectral methods），262  
示例（example），241  暂停动画（suspended animation），265–266  
测地线卷积神经网络（geodesic CNN），265  图形处理器（graphics processing unit），107  
MoNet，265  GraphNorm，265  
幽灵批归一化（ghost batch normalization），203  GraphSAGE，263  
GLIDE，371  GraphSAINT，264  
全局最小值（global minimum），81  GResNet，263  
Glorot 初始化（Glorot initialization），113  概念顿悟（grokking），413  
GLOW，319–321、324  分组归一化（group normalization），203  
“恰到好处”区域（Goldilocks zone），411、413  分组卷积（grouped convolution），181  
GoogLeNet，181  引导卷积（guided convolution），183  
GPT-3，222–227  
解码（decoding），223  HardSwish，38  
少样本学习（few-shot learning），224  He 初始化（He initialization），110、113  
GPU，107  Heaviside 函数（Heaviside function），104  
梯度检查点（gradient checkpointing），114  异方差回归（heteroscedastic regression），64、73  
梯度下降（gradient descent），77–78、91  隐藏层（hidden layer），35  
GradInit，113  隐藏单元（hidden unit），27、35  
图（graph）  隐变量（hidden variable），参见潜在变量（latent variable）  
邻接矩阵（adjacency matrix），243–245  层次化图（hierarchical graph），241  
伴随（adjoint），260  高速公路网络（highway network），202  
边（edge），240、260  Hogwild!，114  
有向边（directed），241  同方差回归（homoscedastic regression），64  
嵌入（embedding），243  沙漏网络（hourglass network），179、197、205  
无向边（undirected），241  堆叠式沙漏网络（stacked），198  
示例（examples），240  Hutchinson 迹估计器（Hutchinson trace estimator），317  
扩展问题（expansion problem），254  超带（Hyperband），136  
几何图（geometric），241  超网络（hypernetwork），235  
异质图（heterogenous），241  超参数（hyperparameter），46  
层次化图（hierarchical），241  超参数模型（hyperparameter model），46  
知识图（knowledge），241  训练算法（training algorithm），91  
线图（line），260  超参数搜索（hyperparameter search），132、133、135  
最大池化聚合（max pooling aggregation），258  贝叶斯优化（Bayesian optimization），135  
邻域采样（neighborhood sampling），254  β-Bernoulli 赌博机（beta-Bernoulli bandit），135  
节点（node），240  BOHB，136  
嵌入（embedding），243、244  超带（Hyperband），136  
划分（partitioning），254  随机采样（random sampling），135  

本作品受知识共享署名-非商业性使用-禁止演绎（CC-BY-NC-ND）许可协议约束。（C）麻省理工学院出版社。

索引 519  
SMAC，136　逐元素流（elementwise flow），310–311  
树形帕森估计器（Tree-Parzen estimators），136　线性流（linear flow），309–310  
残差流（residual flow），314–317，324  
i.i.d.，参见“独立同分布”（independent and identically distributed）　可逆残差网络（iResNet），315–317，324  
单位矩阵（identity matrix），446　可逆残差网络（iRevNet），314–315，324  
图像插值（image interpolation），11  
图像翻译（image translation），291–296，302　雅可比矩阵（Jacobian），448  
ImageGPT，229　亚诺西池化（Janossy pooling），263  
Imagen，371　詹森不等式（Jensen’s inequality），331  
ImageNet 分类（ImageNet classification），174–176，181　詹森–香农散度（Jensen-Shannon divergence），461  
隐式伦理智能体（implicit ethical agent），425　联合概率（joint probability），449  
隐式正则化（implicit regularization），143，156–157  
重要性采样（importance sampling），340　k 折交叉验证（k-fold cross-validation），134  
Inception 模块（inception block），181　k 跳邻域（k-hop neighborhood），254  
Inception 分数（inception score），272　核（kernel），163  
　　尺寸（size），163  
独立性（independence），452　密钥（key），210  
独立同分布（independent and identically distributed），58　基普夫归一化（Kipf normalization），258，263  
归纳偏置（inductive bias），129　KL 散度，参见“库尔贝克–莱伯勒散度”（Kullback-Leibler divergence）  
　　卷积归纳偏置（convolutional），170　知识蒸馏（knowledge distillation），416，419  
　　关系归纳偏置（relational），248　知识图谱（knowledge graph），241  
归纳模型（inductive model），252　库尔贝克–莱伯勒散度（Kullback-Leibler divergence），71，461  
　　正态分布间的 KL 散度（between normals），462  
推理（inference），17　Lᵏ 池化（Lᵏ pooling），181  
InfoGAN，291　L∞ 范数（L-infinity norm），443  
信息偏好问题（information preference problem），346　L₀ 正则化（L₀ regularization），155  
初始化（initialization），107–111　L₁ 正则化（L₁ regularization），156  
　　ActNorm 初始化，113　L₂ 范数（L₂ norm），443  
　　卷积层初始化（convolutional layers），183　L₂ 正则化（L₂ regularization），140  
　　卷积正交初始化（Convolution Orthogonal），183　标签（label），64  
　　Fixup 初始化，205　标签平滑（label smoothing），149，158  
　　Glorot 初始化，113　语言模型（language model），222，234  
　　GradInit 初始化，113　　少样本学习（few-shot learning），224  
　　He 初始化，113　　GPT-3，222–227  
　　层序单位方差初始化（layer-sequential unit variance），113　　大语言模型（large language model），224，234  
　　LeCun 初始化，113　LASSO，155，156  
　　SkipInit 初始化，205　潜在空间（latent space）  
　　TFixup 初始化，237　　解耦潜在空间（disentangled），271  
　　Xavier 初始化，113　潜在变量（latent variable），7，269  
内对齐问题（inner alignment problem），422　潜在变量模型（latent variable model），327  
　　高斯混合模型（mixture of Gaussians），328  
　　非线性模型（nonlinear），328  
修复（inpainting），8　层（layer），35  
实例归一化（instance normalization），203　　卷积层（convolutional），161，165  
内部协变量偏移（internal covariate shift），203　　隐藏层（hidden），35  
可解释性，参见“可解释性”（explainability）　　输入层（input），35  
交叉性（intersectionality），424　　可逆层（invertible），309  
不变性（invariance），161　　　自回归流（autoregressive flow），312–314  
　　排列不变性（permutation），162，213，249　　　耦合流（coupling flow），311–312  
　　旋转不变性（rotation），182　　　逐元素流（elementwise flow），310–311  
　　尺度不变性（scale），182　　　线性流（linear flow），309–310  
　　平移不变性（translation），182　　　残差流（residual flow），314–317，324  
逆自回归流（inverse autoregressive flow），314　　输出层（output），35  
矩阵的逆（inverse of a matrix），444　　残差层（residual），189  
可逆层（invertible layer），309　层归一化（layer normalization），203  
　　自回归流（autoregressive flow），312–314，324　层序单位方差初始化（layer-sequential unit variance initialization），113  

草稿：请将勘误发送至 udlbookmail@gmail.com。

520 索引  
layer-wise DropEdge，265　流形精确率/召回率（manifold precision/recall），274  
leaky ReLU，38　边缘化（marginalization），450  
学习（learning），18　马尔可夫链（Markov chain），351  
学习率（learning rate），78　马尔可夫决策过程（Markov decision process），378  
学习率调度（learning rate schedule），86　马尔可夫过程（Markov process），374  
学习率预热（learning rate warmup），93　马尔可夫奖励过程（Markov reward process），374  
排序学习（learning to rank），73　掩码自回归流（masked autoregressive flow），313  
最小二乘 GAN（least squares GAN），300　掩码自注意力（masked self-attention），223  
最小二乘损失（least squares loss），19，62　矩阵（matrix），437，443  
LeCun 初始化（LeCun initialization），113　矩阵微积分（matrix calculus），448  
LeNet，180　列空间（column space），444  
似然（likelihood），58，451，452　行列式（determinant），445  
似然比恒等式（likelihood ratio identity），390　特征值（eigenvalue），445  
LIME，427　逆矩阵（inverse），444  
线图（line graph），260　雅可比矩阵（Jacobian），448  
线搜索（line search），92　置换矩阵（permutation），245  
线性代数（linear algebra），447　矩阵乘积（product），444  
线性流（linear flow），309–310，323　奇异矩阵（singular），444  
线性函数（linear function），27，447　奇异值（singular value），445  
线性规划（linear programming），285　特殊类型矩阵（special types），446  
线性回归（linear regression），18　对角矩阵（diagonal），446  
LinFormer，237　单位矩阵（identity），446  
利普希茨常数（Lipschitz constant），440  
下三角矩阵（lower triangular），446  
局部注意力（local attention），237　正交矩阵（orthogonal），447  
局部极小值（local minimum），81　置换矩阵（permutation），447  
实值损失函数中的局部极小值（in real loss functions），411　上三角矩阵（upper triangular），446  
对数似然（log-likelihood），59　迹（trace），445  
对数（logarithm），441　转置（transpose），443  
逻辑回归（logistic regression），94　最大值函数（max function），438  
物流 sigmoid（logistic sigmoid），66　最大池化（max pooling），171，181  
损失（loss），19–21　最大池化聚合（max pooling aggregation），258  
对抗性损失（adversarial），293　最大反池化（max unpooling），172，181  
感知损失（perceptual），293　MaxBlurPool，182  
VGG 损失，293　最大后验准则（maximum a posteriori criterion），139  
损失函数（loss function），21，56–76　最大似然估计（maximum likelihood），56–59  
二元交叉熵（binary cross-entropy），66　均值（mean），455  
凸损失函数（convex），80　平均池化（mean pooling），171，246  
交叉熵（cross-entropy），71–72　性能度量（measuring performance），118–137  
焦点损失（focal），73　中位数估计（median estimation），73  
全局极小值（global minimum），81　内存压缩注意力（memory-compressed attention），237  
最小二乘（least squares），19，62　微批次（micro-batching），114  
局部极小值（local minimum），81　军事化（militarization），428  
多类交叉熵（multiclass cross-entropy），69　min 函数（min function），438  
负对数似然（negative log-likelihood），60　小批量（mini-batch），85  
非凸损失函数（non-convex），80　歧视（discrimination），289，301  
分位数损失（pinball），73　极小极大博弈（minimax game），278  
损失函数的性质（properties of），407–411　量化损失（quantile），73  
最小值（minimum），81　连接关系（connections between），408  
损失函数族（family of），408  
通向极小值的路径（route to），408  
彩票假设（lottery ticket），407，416　误用（misuse），427  
下三角矩阵（lower triangular matrix），446　数据隐私（data privacy），429  
ℓp 范数（ℓp norm），443　人脸识别（face recognition），427  
欺诈（fraud），428　军事化（militarization），428  
流形（manifold），274  

本作品受知识共享署名-非商业性使用-禁止演绎（CC-BY-NC-ND）许可协议约束。（C）麻省理工学院出版社。

索引 521  
政治干预，428  
Nesterov 加速动量（Nesterov accelerated momentum），86，92  
MixHop，263  
网络（network），参见神经网络（neural network）  
混合密度网络（mixture density network），74  
网络解剖（network dissection），184  
混合模型网络（mixture model network），263，265  
网络反演（network inversion），184  
高斯混合模型（mixture of Gaussians），75，328  
网络中的网络（network-in-network），181  
多层感知机（MLP），35  
神经架构搜索（neural architecture search），132  
MNIST，292  
神经网络（neural network）  
浅层神经网络（shallow），25–40  
偏置（bias），36  
容量（capacity），29，46  
胶囊网络（capsule），235  
组合式网络（composing），41  
卷积神经网络（convolutional），161–185  
深度神经网络（deep），41–55  
深度 vs. 浅层（deep vs. shallow），49–51  
深度（depth），46  
深度效率（depth efficiency），50，53  
编码器–解码器网络（encoder-decoder），179  
前馈神经网络（feed-forward），35  
全连接网络（fully connected），36  
图神经网络（graph），240–268  
高速公路网络（highway），202  
历史（history），37  
沙漏网络（hourglass），179，197  
超参数（hyperparameter），46  
层（layer），35  
矩阵表示法（matrix notation），49  
多头自注意力机制（multi-head self-attention），214  
循环神经网络（recurrent），233  
多层感知机（multi-layer perceptron），35  
残差网络（residual），186，206  
堆叠沙漏网络（stacked hourglass），198  
Transformer 网络（transformer），207–239  
U-Net，197  
权重（weights），36  
宽度（width），46  
宽度效率（width efficiency），53  
神经常微分方程（neural ODE），202  
牛顿法（Newton method），92  

NLP，参见自然语言处理（natural language processing）  

NAdam，92  
节点（node），240  
节点嵌入（node embedding），243，244  
噪声（noise），122  
向输入添加噪声（adding to inputs），149  
向权重添加噪声（adding to weights），149  
噪声条件增强（noise conditioning augmentation），370  
噪声调度（noise schedule），350  
含噪声的深度 Q 网络（noisy deep Q-network），398  
非凸函数（non-convex function），80  
非负齐次性（non-negative homogeneity），39  
非线性函数（nonlinear function），27  
非线性潜在变量模型（nonlinear latent variable model），328  
范数（norm）  
ℓ<sub>p</sub> 范数，443  
欧几里得范数（Euclidean），443  
谱范数（spectral），445  
向量范数（vector），443  
权重范数（norm of weights），414  

Draft：请将勘误发送至 udlbookmail@gmail.com。

522 索引  
正态分布（normal distribution），61，457–459　　独热向量（one-hot vector），218，245  
变量替换（change of variable），459　　不透明度（opacity），参见“透明度（transparency）”  
距离（distance between），462　　优化（optimization）  
Fréchet 距离（Fréchet distance between），462　　AdaDelta，93  
KL 散度（KL divergence between），462　　AdaGrad，93  
多元正态分布（multivariate），457　　Adam，93  
两个正态分布的乘积（product of two normals），459　　AdamW，94  
采样（sampling），460　　算法（algorithm），77  
标准正态分布（standard），457　　梯度下降法（gradient descent），91  
单变量正态分布（univariate），457　　学习率（learning rate）  
Wasserstein 距离（Wasserstein distance between），462　　　预热（warmup），93  
归一化（normalization）　　　线搜索（line search），92  
批归一化（batch），192–194　　AMSGrad，93  
蒙特卡洛归一化（Monte Carlo），203　　NAdam，92  
批重归一化（batch renormalization），203　　牛顿法（Newton method），92  
幽灵批归一化（ghost batch），203　　目标函数（objective function），91  
组归一化（group），203　　RMSProp，93  
图神经网络中的归一化（in graph neural networks），265　　随机梯度下降法（SGD），91  
实例归一化（instance），203　　随机方差缩减下降法（stochastic variance reduced descent），91  
Kipf 归一化（Kipf），258，263　　YOGI，93  
层归一化（layer），203  
标准化流（normalizing flows），304–326　　正交矩阵（orthogonal matrix），447  
应用（applications），323　　外部对齐问题（outer alignment problem），422  
自回归型（autoregressive），312–314，324　　输出函数（output function），参见“损失函数（loss function）”  
耦合型（coupling），324　　过平滑（over-smoothing），265  
耦合流（coupling flow），311–312　　过拟合（overfitting），22，125  
耦合函数（coupling functions），323　　过参数化（overparameterization），405，415  
逐元素型（elementwise），310–311，323  
生成方向（generative direction），306　　填充（padding），164  
GLOW，319–321，324　　PairNorm，265  
变分推断中的标准化流（in variational inference），321　　参数（parameter），18，437  
无穷小型（infinitesimal），325　　参数化 ReLU（parametric ReLU），38  
逆自回归型（inverse autoregressive），314　　部分卷积（partial convolution），181  
线性型（linear），309–310，323　　部分可观测马尔可夫决策过程（partially observable MDP），378  
掩码自回归型（masked autoregressive），313　　PatchGAN，292  
多尺度型（multi-scale），317–318　　主成分分析（PCA），345  
标准化方向（normalizing direction），306　　感知机（perceptron），37  
平面流（planar），323　　感知损失（perceptual loss），293  
径向流（radial），323　　性能（performance），118–137  
残差型（residual），314–317，324　　Performer，237  
iResNet，315–317　　置换不变性（permutation invariance），162，213，249  
iRevNet，314–315　　置换矩阵（permutation matrix），245，447  
通用性（universality），325　　分位数损失（pinball loss），73  
记号（notation），437–439　　流水线模型并行（pipeline model parallelism），114  
零空间（null space），444　　关键调优（pivotal tuning），302  
数集（number set），437　　Pix2Pix，292  
PixelShuffle，182  
目标检测（object detection），177，183　　PixelVAE，345  
特征金字塔网络（feature pyramid network），183　　平面流（planar flow），323  
基于提议的方法（proposal based），183　　泊松分布（Poisson distribution），76  
无提议方法（proposal free），184　　策略（policy），378  
R-CNN，183　　　行为策略（behavior），385  
YOLO，177，184　　　玻尔兹曼策略（Boltzmann），400  
目标函数（objective function），参见“损失函数（loss function）”　　　ε-贪心策略（epsilon-greedy），385  
离策略方法（off-policy method），385　　　目标策略（target），385  
离线强化学习（offline reinforcement learning），395　　策略梯度法（policy gradient method），389  
在线策略方法（on-policy method），385　　近端策略优化（PPO），398  

本作品受知识共享署名-非商业性使用-禁止演绎（CC-BY-NC-ND）许可协议约束。（C）麻省理工学院出版社。

索引 523  
REINFORCE，392　深度型，386–388  
TRPO，398　双网络型，388  
策略网络（policy network），12　双重深度型，388  
政治干预（political interference），428　拟合型，385  
部分可观测马尔可夫决策过程（POMDP），378　含噪声的深度 Q 网络（noisy deep Q-network），398  
池化（pooling）　分位数损失（quantile loss），73  
ℓₖ，181　分位数回归（quantile regression），73  
平均池化（average），181　查询（query），210  
图神经网络中的池化（in graph neural networks），265　问答（question answering），222  
Janossy 池化，263  
最大池化（max），181　R-CNN，183  
最大模糊池化（max-blur），181　Rademacher 复杂度，134  
位置编码（positional encoding），213，236　径向流（radial flow），323  
后验分布（posterior），452　Rainbow，398  
后验坍缩（posterior collapse），346　随机合成器（random synthesizer），235  
近端策略优化（PPO），398　随机变量（random variable），449  
预激活（pre-activation），35　RandomDrop，202  
预激活残差块（pre-activation residual block），201　排序（ranking），73  
预训练（pre-training），151　召回率（recall），274  
Transformer 编码器预训练（transformer encoder），219　感受野（receptive field），167  
精确率（precision），274　图神经网络中的感受野（in graph neural networks），254  
委托-代理问题（principal agent problem），422　重构损失（reconstruction loss），335  
先验分布（prior），139，140，452　修正 Adam（rectified Adam），93  
先验偏移（prior shift），135　修正线性单元（rectified linear unit，ReLU），25  
优先经验回放（prioritized experience replay），397　其导数（derivative of），104  
概率生成模型（probabilistic generative model），270　ReLU 死亡问题（dying ReLU problem），38  
概率主成分分析（probabilistic PCA），345　非负齐次性（non-negative homogeneity），39  
概率（probability），449–462　循环 Dropout（recurrent dropout），158  
贝叶斯法则（Bayes’ rule），451　循环神经网络（recurrent neural network，RNN），233  
条件概率（conditional），450　回归（regression），2  
概率密度函数（probability density function），449　异方差回归（heteroscedastic），73  
概率分布（probability distribution），438，449　多元回归（multivariate），2，69  
伯努利分布（Bernoulli），65　分位数回归（quantile），73  
分类分布（categorical），67　鲁棒回归（robust），73  
连续型分布（continuous），449　单变量回归（univariate），61  
离散型分布（discrete），449　正则化（regularization），131，138–160  
分布间距离（distance between），460–462　AdamW，155  
高斯混合分布（mixture of Gaussians），328　添加噪声（adding noise），158  
多元正态分布（multivariate normal），457　向输入添加噪声（adding noise to inputs），149  
正态分布（normal），457–459　向权重添加噪声（adding noise to weights），149  
泊松分布（Poisson），76　对抗训练（adversarial training），149  
从分布中采样（sampling from），460　数据增强（augmentation），152–154  
单变量正态分布（univariate normal），61，457　Bagging，146  
von Mises 分布，74　贝叶斯方法（Bayesian approaches），150  
联合分布（joint），449　数据增强（data augmentation），159  
边缘化（marginalization），450　DropConnect，265  
记号（notation），438　DropEdge，265  
随机变量（random variable），449　Dropout，147  
概率流常微分方程（probability flow ODE），371　早停法（early stopping），145，157  
渐进式增长（progressive growing），287，301　弹性网络（elastic net），156  
受保护属性（protected attribute），424　集成学习（ensemble），145，157  
近端策略优化（proximal policy optimization），398　泛滥法（flooding），159  
剪枝（pruning），415　Frobenius 范数（Frobenius norm），140，155  
隐式正则化（implicit），141，143，156–157  
图神经网络中的隐式正则化（in graph neural networks），265  
L₀，155  
Q 学习（Q-learning），385　L₁，156  

草稿：请将勘误发送至 udlbookmail@gmail.com。

524 索引  
L2 正则化，140　　return（回报），375  
标签平滑（label smoothing），149，158　　reward（奖励），375  
LASSO，156　　rollout（ rollout ），382  
多任务学习（multi-task learning），151　　SARSA，385  
概率解释（probabilistic interpretation），139　　状态值（state value），378  
RandomDrop，202　　状态-动作值函数（state-action value function），379  
ResDrop，202　　状态值函数（state-value function），379  
岭回归（ridge regression），140　　表格型方法（tabular），382–385  
自监督学习（self-supervised learning），159　　时序差分法（temporal difference method），385  
ShakeDrop，203　　轨迹（trajectory），382  
Shake-Shake，203　　value（值），375  
随机深度（stochastic depth），202　　基于人类反馈的强化学习（with human feedback），399  
Tikhonov 正则化，140　　关系归纳偏置（relational inductive bias），248  
迁移学习（transfer learning），151，159　　ReLU，参见修正线性单元（rectified linear unit）  
权重衰减（weight decay），140　　重参数化技巧（reparameterization trick），339，347  
与 L2 正则化的对比（vs. L2），155　　表征能力（representational capacity），134  
REINFORCE，392　　ResDrop，202  
强化学习（reinforcement learning），374–401　　残差块（residual block），189  
动作值（action value），378　　运算顺序（order of operations），191  
优势函数（advantage function），394　　残差连接（residual connection），189  
基线（baseline），392　　在图神经网络中的应用（in graph neural networks），263，266  
批处理（batch），395　　为何提升性能（why improves performance），202  
贝尔曼方程（Bellman equations），380　　残差流（residual flow），314–317，324  
经典方法（classical），397　　iResNet，315–317  
致命三重奏问题（deadly triad issue），397　　iRevNet，314–315  
深度双网络（deep dueling network），398　　残差网络（residual network），186–206  
折扣因子（discount factor），375　　作为集成方法（as ensemble），202  
动态规划方法（dynamic programming methods），383　　性能（performance），198  
回合（episode），382，384　　稳定 ResNet（stable ResNet），205  
经验回放（experience replay），387　　展开（unraveling），189  
探索-利用权衡（exploration-exploitation trade-off），374　　ResNet v1 与 v2，201  
组合优化中的应用（for combinatorial optimization），397　　ResNet-200，195  
引言（introduction），11–12　　ResNeXt，202  
马尔可夫决策过程（Markov decision process），378　　重合成（resynthesis），342  
蒙特卡洛方法（Monte Carlo method），382，384　　return（回报），375  
自然策略梯度（natural policy gradients），398　　反向模式微分（reverse-mode differentiation），116  
离线强化学习（offline），395　　reward（奖励），375  
策略（policy），378　　岭回归（ridge regression），140  
行为策略（behavior），385　　RL，参见强化学习（reinforcement learning）  
玻尔兹曼策略（Boltzmann），400　　RMSProp，93  
ε-贪心策略（epsilon-greedy），385　　RNN，233  
最优策略（optimal），379　　鲁棒回归（robust regression），73  
目标策略（target），385　　rollout（rollout），382  
策略梯度法（policy gradient method），389　　旋转等变性（rotation equivariance），182  
PPO，398　　旋转不变性（rotation invariance），182  
REINFORCE，392　　运行透明性（run transparency），426  
TRPO，398  
策略网络（policy network），12　　鞍点（saddle point），81，83，91  
部分可观测马尔可夫决策过程（POMDP），378　　采样（sampling），460  
优先经验回放（prioritized experience replay），397　　祖先采样（ancestral），460  
Q 学习（Q-learning），385　　从多元正态分布采样（from multivariate normal），460  
深度 Q 网络（deep Q-network），386–388，397　　SARSA，385  
双 DQN（double DQN），388　　标量（scalar），437  
双 Q 学习（double Q-learning），388　　尺度不变性（scale invariance），182  
拟合 Q 迭代（fitted），385　　缩放点积自注意力（scaled dot-product self-attention），214  
含噪声深度 Q 网络（noisy deep Q-network），398　　缩放指数线性单元（scaled exponential linear unit），113  
Rainbow，398　　科学交流（scientific communication），433  

本作品受知识共享署名-非商业性使用-禁止演绎（CC-BY-NC-ND）许可协议约束。（C）麻省理工学院出版社。

索引 525  
分割（segmentation），178　状态值（state value），378  
U-Net，199　状态–动作值函数（state-action value function），379  
自注意力（self-attention），208–215　状态值函数（state-value function），379  
作为路由机制的自注意力（as routing），235　斯特林公式（Stirling’s formula），441  
编码器–解码器（encoder-decoder），227　随机深度（stochastic depth），202  
键（key），210　随机梯度下降（stochastic gradient descent），83–86，91，404  
掩蔽自注意力（masked），223　全批量梯度下降（full batch），85  
矩阵表示（matrix representation），212　性质（properties），85  
多头注意力（multi-head），214　随机方差缩减下降（stochastic variance reduced descent），91  
位置编码（positional encoding），213　随机权重平均（stochastic weight averaging），158  
查询（query），210　步长（stride），164  
缩放点积注意力（scaled dot-product），214　结构透明性（structural transparency），426  
值（value），208　结构化数据（structured data），17  
自监督学习（self-supervised learning），152，159　StyleGAN，296–298，301  
对比式（contrastive），152　子词分词器（sub-word tokenizer），218  
生成式（generative），152　子空间（subspace），444  
SeLU，113　超分辨率（super-resolution），293  
语义分割（semantic segmentation），178，184　监督学习（supervised learning），1–7，17–24  
半监督学习（semi-supervised learning），252　满射（surjection），440  
SentencePiece，234　暂停动画（suspended animation），265–266  
情感分析（sentiment analysis），221　SWATS，94  
可分离卷积（separable convolution），181　SWin Transformer，232，238  
分离性（separation），424　v2 版本，238  
序列到序列任务（sequence-to-sequence task），226　Synthesizer，235  
集合（set），437　随机 Synthesizer（random），235  
SGD，参见随机梯度下降（stochastic gradient descent）　表格数据（tabular data），2，17  
表格强化学习（tabular reinforcement learning），382–385  
shake-drop，203　目标策略（target policy），385  
shake-shake，203　教师强制（teacher forcing），234  
浅层神经网络（shallow neural network），25–40，41　技术沙文主义（techno-chauvinism），434  
破碎梯度（shattered gradients），187　技术性失业（technological unemployment），430  
跳跃连接（skip connection），参见残差连接（residual connection）　时序差分法（temporal difference method），382，385  
SkipInit，205　张量（tensor），107，437，443  
Slerp，342　张量模型并行（tensor model parallelism），114  
SMAC，136　TensorFlow，106  
快照集成（snapshot ensembles），158　测试数据（test data），22  
Softmax，68　测试误差（test error）  
Softplus，38　偏差（bias），122  
稀疏性（sparsity），155　双重下降（double descent），127  
空间 Dropout（spatial dropout），183　噪声（noise），122  
谱范数（spectral norm），445　方差（variance），122  
球面协方差矩阵（spherical covariance matrix），457　测试集（test set），118  
球面线性插值（spherical linear interpolation），342　文本分类（text classification），221  
SQuAD 问答任务（SQuAD question answering task），222　文本合成（text synthesis），8，224–225  
条件式（conditional），9  
SRGAN，293　文本到图像（text-to-image），368，371  
Stable Diffusion，370　TFixup，237  
稳定 ResNet（stable ResNet），205　蒂科诺夫正则化（Tikhonov regularization），140  
堆叠沙漏网络（stacked hourglass network），198，205　分词（tokenization），218，234  
堆叠（stacking），157　BPE Dropout，234  
标准差（standard deviation），455　字节对编码（Byte Pair Encoding），234  
标准正态分布（standard normal distribution），457　SentencePiece，234  
标准化（standardization），456　子词（sub-word），218  
立场认识论（standpoint epistemology），434　WordPiece，234  

草稿：请将勘误发送至 udlbookmail@gmail.com。

526 索引  
top-k 采样（top-k sampling），224　函数式（functional），426  
总相关性 VAE（total correlation VAE），346　运行（run），426  
迹（trace），445　结构式（structural），426  
Hutchinson 估计器（Hutchinson estimator），317　传输计划（transport plan），284  
训练（training），5, 18, 77–117　转置（transpose），443  
批处理（batch），85　转置卷积（transposed convolution），172, 181  
轮次（epoch），85　树形 Parzen 估计器（Tree-Parzen estimators），136  
误差（error），19　三角矩阵（triangular matrix），446  
决定成功与否的因素（factors that determine success），403–407　信赖域策略优化（TRPO），398  
梯度检查点技术（gradient checkpointing），114　截断技巧（truncation trick），289  
微批次（micro-batching），114　信赖域策略优化（trust region policy optimization），398  
降低内存需求（reducing memory requirements），114  
随机梯度下降（stochastic gradient descent），83–86　U-Net，197, 205  
`++`，205　3D，205  
分割结果（segmentation results），199  
归纳式模型（transductive model），252　欠拟合（underfitting），22  
迁移学习（transfer learning），151, 159, 219　无向边（undirected edge），241  
微调（fine-tuning），151　单变量正态分布（univariate normal），457  
预训练（pre-training），151　单变量回归（univariate regression），61  
Transformer，207–239　通用近似定理（universal approximation theorem），29  
应用（applications），234　深度（depth），53  
应用于图像（applied to images），228–232　宽度（width），38  
BERT，219–222　普适性（universality）  
BigBird，237　归一化流的普适性（normalizing flows），325  
CLIP，238　反池化（unpooling），181  
与 CNN 结合（combined with CNNs），238　展开（unraveling），189  
图文联合建模（combining images and text），238　无监督学习（unsupervised learning），7–11, 269–275  
交叉协方差图像 Transformer（cross covariance image transformer），238　模型分类法（model taxonomy），269  
Crossformer，238　上三角矩阵（upper triangular matrix），446  
DaViT，232, 238　上采样（upsample），171  
解码算法（decoding algorithm），234  
定义（definition），215  
编码器模型（encoder model），219–222　V-Net，205  
编码器–解码器模型（encoder-decoder model），226　VAE（参见变分自编码器，variational autoencoder）  
扩展构造（extended construction），237　有效卷积（valid convolution），164  
用于自然语言处理（for NLP），216　值（value），208, 375  
用于视频处理（for video processing），238　价值对齐（value alignment），421–427  
用于计算机视觉（for vision），238　内对齐问题（inner alignment problem），422  
ImageGPT，229　外对齐问题（outer alignment problem），422  
LinFormer，237　委托–代理问题（principal agent problem），422  
长序列（long sequences），227　动作值（value of action），378  
多头自注意力机制（multi-head self-attention），214　状态值（value of state），378  
多尺度（multi-scale），238　科学的价值中立理想（value-free ideal of science），432  
多尺度视觉（multi-scale vision），230　梯度消失问题（vanishing gradient problem），108  
Performer，237　残差网络中的梯度消失（in residual networks），192  
位置编码（positional encoding），213, 236　Vapnik–Chervonenkis 维数（Vapnik-Chervonenkis dimension），134  
金字塔视觉（pyramid vision），238　变量（variable），437  
缩放点积注意力（scaled dot-product attention），214　方差（variance），122, 455  
SWin，232, 238　单位方差（identity），455  
SWinV2，238　变分近似（variational approximation），336  
Synthesizer，235　结合归一化流的变分近似（with normalizing flows），321  
TFixup，237　变分自编码器（variational autoencoder），327–348  
ViT，229　对抗式学习推断（adversarially learned inference），346  
翻译（自动）（translation (automatic)），226　聚合后验（aggregated posterior），341, 342  
平移等变性（translation equivariance），182　应用（applications），344  
平移不变性（translation invariance），182　β-VAE，343, 346  
透明性（transparency），425–426　与其他模型的结合（combination with other models），346  

本作品受知识共享署名–非商业性使用–禁止演绎（CC-BY-NC-ND）许可协议约束。（©）麻省理工学院出版社。

索引 527  
条件分布（conditional），345；零填充（zero-padding），164  
解码器（decoder），338  
解耦（disentanglement），342  
编码器（encoder），338  
概率估计（estimating probability），340  
因子变分自编码器（factor VAE），347  
生成（generation），341  
后验分布的分层模型（hierarchical model for posterior），345  
潜在空间中的空洞（holes in latent space），346  
信息偏好问题（information preference problem），346  
似然项的修改（modifying likelihood term），345  
归一化流（normalizing flows），345  
PixelVAE，345  
后验坍缩（posterior collapse），346  
与EM算法的关系（relation to EM algorithm），347  
与其他模型的关系（relation to other models），345  
重参数化技巧（reparameterization trick），339，347  
重构合成（resynthesis），342  
总相关性变分自编码器（total correlation VAE），346  
VC维（VC dimension），134  
向量（vector），437，443  
点积（dot product），444  
范数（norm），443  
VEEGAN，301  
VGG，176  
VGG损失（VGG loss），293  
视觉Transformer（vision transformer）  
　DaViT，232  
　ImageGPT，229  
　多尺度（multi-scale），230  
　Swin Transformer（SWin），232  
　Vision Transformer（ViT），229  
激活值可视化（visualizing activations:），184  
ViT，229  
冯·米塞斯分布（von Mises distribution），74  
Wasserstein距离（Wasserstein distance），283  
　正态分布间的Wasserstein距离（between normals），462  
Wasserstein生成对抗网络（Wasserstein GAN，WassersteinGAN），281–286，300  
人工智能武器化（weaponization of AI），13  
权重（weight），36  
　衰减（decay），140，155  
　初始化（initialization），107–111  
　矩阵（matrix），49  
Weisfeiler-Lehman图同构检验（Weisfeiler-Lehman graph isomorphism test），264  
带梯度惩罚的Wasserstein GAN（WGAN-GP），286  
宽极小值（wide minima），158  
宽度效率（width efficiency），53  
神经网络宽度（width of neural network），46  
词分类（word classification），221  
词嵌入（word embedding），218  
WordPiece，234  
Xavier初始化（Xavier initialization），113  
YOGI，93  
YOLO，177，184  

草稿：请将勘误发送至 udlbookmail@gmail.com。